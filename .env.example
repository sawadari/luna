# GitHub Configuration
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
REPOSITORY=owner/repo

# Anthropic API (required for AI-powered features: NRVV extraction, auto-completion)
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Kernel Registry Configuration
# Default: kernels-luna-base.yaml (Luna's own development)
# For other projects: set to kernels.yaml
# KERNEL_REGISTRY_PATH=kernels.yaml

# Rules Configuration (Human-AI Responsibility Boundary)
# NOTE: rules-config.yaml is now the primary configuration method.
# Environment variables below are for backward compatibility only.
# See RULES_CONFIGURATION.md for details.
#
# DEST Configuration (default: all enabled for safety, opt-out with =false)
# ENABLE_DEST_JUDGMENT=false
# ENABLE_CREPS_GATES=false
# ENABLE_PLANNING_LAYER=false
# ENABLE_SSOT_LAYER=false
#
# Priority Order:
# 1. rules-config.yaml (recommended)
# 2. Environment variables (legacy, fallback)
# 3. Default values

# Agent Configuration
DRY_RUN=false
VERBOSE=true
